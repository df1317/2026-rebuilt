@@ -144,11 +147,11 @@
 		Pose2d tagPose = computeTagShootPose(startPose);
 		Pose2d collectPose = computeCollectPose();
 		return sequence(
-				apfDefaultsFacing(() -> tagPose, HUB_CENTER, 180),
+				apfFastFacing(() -> tagPose, HUB_CENTER, 180),
 				shootInitial(),
 				deadline(
-						apfDefaults(collectPose).andThen(intake.stowCommand()).andThen(apfDefaultsFacing(() -> tagPose, HUB_CENTER, 180)),
+						apfFast(collectPose).andThen(intake.stowCommand()).andThen(apfFastFacing(() -> tagPose, HUB_CENTER, 180)),
 						collect()
 				),
 				shoot());
 	}
@@ -158,16 +161,16 @@
 		Pose2d tagPose = computeTagShootPose(startPose);
 		Pose2d collectPose = computeCollectPose();
 		return sequence(
-				apfDefaultsFacing(() -> tagPose, HUB_CENTER, 180),
+				apfFastFacing(() -> tagPose, HUB_CENTER, 180),
 				shootInitial(),
 				deadline(
-						apfDefaults(collectPose).andThen(intake.stowCommand()).andThen(apfDefaultsFacing(() -> tagPose, HUB_CENTER, 180)),
+						apfFast(collectPose).andThen(intake.stowCommand()).andThen(apfFastFacing(() -> tagPose, HUB_CENTER, 180)),
 						collect()
 				),
 				shoot(),
 				deadline(
-						apfDefaults(collectPose).andThen(intake.stowCommand()).andThen(apfDefaultsFacing(() -> tagPose, HUB_CENTER, 180)),
+						apfFast(collectPose).andThen(intake.stowCommand()).andThen(apfFastFacing(() -> tagPose, HUB_CENTER, 180)),
 						collect()
 				),
 				shoot());
 	}
